<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wkxjc.wanandroid">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".me.language.LanguageActivity" />
        <activity android:name=".me.user.collection.CollectionActivity" />
        <activity android:name=".me.user.todo.TodoActivity" />
        <activity android:name=".me.login.register.RegisterActivity" />
        <activity android:name=".me.login.LoginActivity" />
        <activity android:name=".knowledgeTree.knowledgeTreeArticles.KnowledgeTreeArticlesActivity" />
        <activity android:name=".common.artical.WebActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>